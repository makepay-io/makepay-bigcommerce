# Contributing

Thanks for helping improve the MakePay BigCommerce app.

Run `npm run validate` before opening a pull request. Test OAuth callbacks and webhook handlers with a BigCommerce draft app before promoting changes.

Do not commit BigCommerce app secrets, MakePay partner keys, merchant access tokens, order payloads from production, or webhook signing secrets.
